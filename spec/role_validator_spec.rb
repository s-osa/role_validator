require 'spec_helper'

describe RoleValidator do
  it 'has a version number' do
    expect(RoleValidator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
